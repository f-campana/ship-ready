ShipReady checks launch readiness without changing the project. Enter the live Fodmapp URL and its local project folder, then check the site. Needs attention highlights the next action for crawlers and preview bots without implying a ranking result. Safe apply previews two missing crawl files before any action: app/robots.ts and app/sitemap.ts. Review both files, then copy the guarded creation-only command when ready. The GUI writes nothing. The command does not overwrite files, edit metadata or content, commit code, or deploy. Local changes do not affect the live site until deployment, so review locally, deploy through the normal process, and re-check the live site.
