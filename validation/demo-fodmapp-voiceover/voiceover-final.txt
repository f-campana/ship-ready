Generated websites can look ready while still missing launch-readiness details. ShipReady checks what crawlers and preview bots can see. The report shows when attention is needed before launch. It separates safe crawl files from changes that require review, so developers can inspect the plan first. Here, safe apply contains two files: robots dot T S and sitemap dot T S. The GUI writes no files; it only copies a guarded command for intentional use. That command is creation-only: no overwrites, no metadata or content edits, no Git commits, and no deploys. Review locally and deploy through the normal process. The live site changes only after deployment, so deploy when ready, then check it again.
